#include <iostream>
using namespace std;

int main()
{
    /*          A)
     * j = k > 90 ? 57 : 12;
     *
     *  if (k>90)
     *      j = 57;
     *  else
     *      j = 12;
     *
     *          B)
     *  factor = x >= 10 ? y * 22 : y * 35
     *
     *      if (x >= 10)
     *          factor = y * 22;
     *      else
     *          factor = y * 35;
     *
     *          C)
     *  total += count == 1 ? sales : count * sales;
     *
     *  if (count==1)
     *      total += sales;
     *  else
     *      total += count*sales;
     *
     *          D)
     *  cout << (((num%2) == 0) ? "Even\n" : "Odd\n");
     *
     *  if (num%2)
     *      cout << "Odd\n";
     *  else
     *      cout << "Even\n";
     *
     */

    return 0;
}

