// checkoint 7.1
// 10/7/21
#include <iostream>
#include <string>

using namespace std;

int main()
{
    /* 7.1
    int empNums[100];
    float payRates[25];
    long miles[14];
    string cityName[26];
    double lightYears[1000];
    
    // 7.2

    int readings[-1]; // negative for the array declarator - needs be positive
    float measurements[4.5]; // float as the declarator - needs to be an int
    int size; // implicitly defined array needs to be initialized, else it's missing the declarator and is
                // just an int and not an array of ints
    string names[size]; // size needs to be a constant or constant var

    // 7.3
    
    // 0, 1, 2, 3

    // 7.4
    // declarator defines the size of the array or the number of elements. 
    // The subscript is an identifier for an individual element in the array
    //
    //
    //7.5
    
    // program would check if you were trying to access an array element that is not defined for the array
    // c++ does not perform it

    // 7.6
    int values[5], count;
    for (count = 0; count < 5; count++)
        values[count] = count + 1;
    for (count = 0; count < 5; count++)
        cout << values[count] << endl;
    // Output:
    // 1
    // 2
    // 3
    // 4
    // 5

    // 7.7
    // 
    const int NUM_FISH = 20;
    int fish[NUM_FISH];

    // you must finish this program. It should ask how
    // many fish were caught by fishermen 1-20, and 
    // store this data in the array fish.

    for (int i = 0; i < NUM_FISH; i++)
    {
        cout << "How many fish were caught by Fisherman " << i+1 << "? ";
        cin >> fish[i]
    }

    // 7.8
    //
    int ages[10] = {5, 7, 9, 14, 15, 17, 18, 19, 21, 23};
    float temps[] = {14.7, 16.3, 18.43, 21.09, 17.9, 18.76, 26.7};
    char alpha[] = {'J', 'B', 'L', 'A', '*', '$', 'H', 'M'};
    */
   
    // checkpoint 7.9

    // def for numbers is valid
    // matrix is invalid
    // radii is valid
    // table is invalid
    // codes is valid
    // blanks is invalid


    return 0;
}

